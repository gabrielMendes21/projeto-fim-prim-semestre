<main class="pl-80 pt-8 text-white">
    <h1 class="font-bold text-4xl">Anúncio de gameplays</h1>
    <div class="flex gap-5 mt-5">
        {{#each games}}
            <a href="/games/{{this.id}}" class="flex flex-col-reverse">
                <span>{{this.nome}}</span>
                <img class="rounded-lg w-36" src="{{this.URLImagem}}" alt="">
            </a>
        {{/each}}
    </div>
</main>